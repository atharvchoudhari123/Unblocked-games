<p>This project uses node.js and pnpm. please run the following commands in codespaces/vscode, 1. run pnpm install, 2 run pnpm start. than make the port public and acess the website. if it says to update node.js and pnpm, run the following commands
nvm install --lts && nvm use --lts for node.js and then run npm install -g pnpm@latest and then verfiy that they are updated node -v && pnpm -v</p>


